# Eradication

The documented eradication actions were:

- Removed `BrowserUpdate.exe`.
- Removed the `WinUpdateHelper` scheduled task.
- Reset the employee's credentials.
- Restored the affected workstation from a known-good image.
- Enrolled the employee in multi-factor authentication before re-enabling the account.

These actions addressed the identified endpoint artifacts, credentials, and persistence mechanism.
